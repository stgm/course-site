<div id="search-menu">
	<p role="presentation" class="dropdown-header p-0">Search reference documents and videos</p>
	
	<form class="form-inline mr-3 mt-3 w-100">
		<input id="keyword-search" type="search" autocomplete="off" class="w-100 form-control form-control-sm border-secondary" placeholder="Enter topic name" aria-label="Enter topic name">
	</form>
	
	<div id="search-content" class="mt-3" style="max-height:450px; overflow:auto">
	</div>

	<script>
		var numbers = new Bloodhound({
		  datumTokenizer: Bloodhound.tokenizers.obj.whitespace('num'),
		  queryTokenizer: Bloodhound.tokenizers.whitespace,
		  local: [
		    // { num: 'one' },
// 		    { num: 'two' },
// 		    { num: 'three' },
// 		    { num: 'four' },
// 		    { num: 'five' },
// 		    { num: 'six' },
// 		    { num: 'seven' },
// 		    { num: 'eight' },
// 		    { num: 'nine' },
// 		    { num: 'ten' }
		  ],
		  remote: {
		      url: '/search/autocomplete?query=%QUERY',
		      wildcard: "%QUERY",
		  }
		});

		// initialize the bloodhound suggestion engine
		numbers.initialize();

		// instantiate the typeahead UI
		$('#keyword-search').typeahead(null, {
		  // displayKey: 'num',
		  source: numbers.ttAdapter()
		});
		
		function go_search(ev, suggestion) {
			// $('#keyword-search').readOnly = true;
			$('#keyword-search').typeahead('close');
			$.ajax({ url:"<%= search_query_path %>", dataType: "script", data: "query=" + suggestion})
			$('#search-content').focus();
		}
		
		$('#keyword-search').bind('typeahead:autocomplete', go_search);
		$('#keyword-search').bind('typeahead:select', go_search);
		
		$('#keyword-search').on('input', function(e) {
			if(e.target.value == "") {
				$('#search-content').html("");
			}
		});
		
		$('#keyword-search').on('click', function(e) {
			e.target.select();
		});
		
	</script>
</div>
