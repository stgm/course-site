<div id="content" class="container">

	<header class="page-header">
		<h1>Grading list</h1>
	</header>

	<ul>
		<% @submits.each do |submit| %>
			<li><%= submit.pset.name %> from <%= submit.user.name %> <%= submit.user.uvanetid %></li>
		<% end %>
	</ul>
	
</div>
