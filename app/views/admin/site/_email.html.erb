<%= change_setting_form('mailer_from', 'Address that mail is sent from') %>
<p>Mails will be sent from &lt;<%= AppConfig.mailer_from %>&gt;</p>
<%= toggle_setting_form('send_grade_mails', 'Send grade e-mails from now on (be sure to enter a valid address above!)') %>
