
<%= render partial: 'version', layout: 'modal_card', locals: { title: 'Version', subtitle: nil }%>

<%= render partial: 'git', layout: 'modal_card', locals: { title: 'Git', subtitle: nil }%>

<%= render partial: 'email', layout: 'modal_card', locals: { title: 'E-mail', subtitle: nil } %>

<%= render partial: 'settings', layout: 'modal_card', locals: { title: 'Features', subtitle: nil }%>

<%= render partial: 'hands', layout: 'modal_card', locals: { title: 'Hands', subtitle: nil }%>

<%= render partial: 'grading', layout: 'modal_card', locals: { title: 'Grading', subtitle: nil }%>

<%= render partial: 'webdav', layout: 'modal_card', locals: { title: 'Archiving', subtitle: nil }%>

<%= render partial: 'webhook', layout: 'modal_card', locals: { title: 'Webhook', subtitle: nil }%>

<%= render partial: 'permissions', layout: 'modal_card', locals: { title: 'Permissions', subtitle: nil }%>
