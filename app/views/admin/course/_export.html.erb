<%= link_to 'Export grades XSLX', admin_course_export_grades_path(format: 'xlsx'), class: 'btn btn-primary', data: { turbo: false } %>
<%= link_to 'Export grades overview', admin_course_export_grades_path(format: 'html'), class: 'btn btn-primary', data: { turbo: false } %>
