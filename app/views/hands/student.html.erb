<h4>
	<%= @user.name %><% if @user.group %> <%= @user.group.name %><% end %><br>
	<small>was seen on this site <%= @user.last_seen_at ? time_ago_in_words(@user.last_seen_at) : 'never??' %> ago</small>
</h4>

<p>
	Ask this student what they are working on right now. Let them explain a bit.
	<br>
	If this student is new, please welcome them and show the "Ask" button on the website.
	Refer them to help@mprog.nl for practical matters.
</p>

<%= form_tag(controller: :hands, action: :welcomed, id: @user.id) do %>

<p><textarea class="form-control" rows="4" placeholder="Anything to report about this student?" name="note"></textarea></p>

<p>Moodmeter:</p>
		
<p>
	<div class="btn-group btn-group-justified" data-toggle="buttons">
		<label class="btn btn-default">
			<input required type="radio" name="evaluation" value="🎉" id="option1" autocomplete="off"> 🎉 done
		</label>
		<label class="btn btn-default">
			<input type="radio" name="evaluation" value="👍" id="option2" autocomplete="off"> 👍 strong
		</label>
		<label class="btn btn-default">
			<input type="radio" name="evaluation" value="🚂" id="option2" autocomplete="off"> 🚂 steady
		</label>
		<label class="btn btn-default">
			<input type="radio" name="evaluation" value="😱" id="option3" autocomplete="off"> 😱 slow
		</label>
	</div>
</p>
		
<p><button type="submit" class="btn btn-primary btn-block">I've said "hi" to this student</button></p>

<% end %>
