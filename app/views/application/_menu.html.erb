<div class="dropdown  ml-2">
	<a href="#" class="bg-white rounded-pill p-2 dropdown-toggle" data-toggle="dropdown"><%= icon 'person-fill', size: '24x24', style: 'vertical-align:-6px;' %></a>
	<% if logged_in? %>
	<div class="dropdown-menu dropdown-menu-right" id="user-menu">
		<%= render partial: 'menu_items', locals: { context: :menu } %>
	</div>
	<% end %>
</div>
