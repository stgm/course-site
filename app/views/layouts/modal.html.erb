<turbo-frame id="modal">
  <div class="modal-content">
    <div id="modal-browser-header">
      <% if content_for?(:title) %>
        <%= yield(:title) %>
      <% else %>
        <%= render partial: "admin_modal_tabs" %>
      <% end %>
      <button
        type="button"
        class="btn-close"
        data-bs-dismiss="modal"
        aria-label="Close"
      ></button>
    </div>
    <div id="modal-browser-body">
      <%# the app layout's flash sits outside the turbo frame, so a redirect that
          lands back in the modal would drop its message on the floor %>
      <%= render partial: "alerts" %>
      <%= yield %>
    </div>
    <% if content_for?(:footer) %>
      <div id="modal-browser-footer">
        <%= yield(:footer) %>
      </div>
    <% end %>
  </div>
</turbo-frame>
